
1) Install pyenv into /opt/pyenv
########################################################################

# cd /opt
# git clone https://github.com/yyuu/pyenv.git 
# cd pyenv/
# export PYENV_ROOT=/opt/pyenv
# export PATH=/opt/pyenv/bin/:$PATH
# eval "$(pyenv init -)"

Ensure that development libraries are installed. Ex

# yum install readline-devel
# yum install sqlite-devel

installing python 2.7.8 and 3.4.2 (stable versions)

# pyenv install 2.7.8
# pyenv install 3.4.2
# pyenv rehash

Setting the default python as 2.7.8 (only with pyenv)

# pyenv global 2.7.8

To use pyenv into your bash terminal, export those lines in your .bashrc

export PYENV_ROOT=/opt/pyenv
export PATH=/opt/pyenv/bin/:$PATH
eval "$(pyenv init -)"

2) install library dependencies
########################################################################

Now install python lybraries (with the default python of pyenv -2.7.8)

# pip install mako
# pip install mysqlclient
# pip install biopython
